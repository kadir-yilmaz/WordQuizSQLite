# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'WordQuizSQLite' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'SQLite.swift', '~> 0.14.0'

  # Pods for WordQuizSQLite

  target 'WordQuizSQLiteTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WordQuizSQLiteUITests' do
    # Pods for testing
  end

end
